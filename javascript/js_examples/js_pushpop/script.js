//PUSH && POP

var colors = ["red", "orange", "yellow"];
//push adds an element in an array as last item;
colors.push("green");
colors.push("violet");
//pop removes the last element in an array;
// colors.pop();
console.log(colors);
